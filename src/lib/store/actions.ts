import { ActionTree } from 'vuex'
// import { Mutations } from './mutations'
import { State } from './state'

export enum Actions {}
//play = 'play',

export const actions: ActionTree<State, State> = {
  // [Actions.play]({ commit }) {
  //   transportService.start()
  //   commit(Mutations.setIsPlaying, true)
  // },
}
